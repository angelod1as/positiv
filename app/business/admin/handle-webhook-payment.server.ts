import type { AsaasWebhookPayload } from "~/integrations/asaas/types"

export async function handleWebhookPayment(
  _payload: AsaasWebhookPayload,
): Promise<void> {
  // Will be implemented in Phase C
}
