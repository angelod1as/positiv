name: TODO Counter

on:
  pull_request:
    types: [opened, synchronize]

permissions:
  pull-requests: write
  contents: read

jobs:
  count_todos:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout base branch
        uses: actions/checkout@v4
        with:
          ref: ${{ github.base_ref }}
          fetch-depth: 0

      - name: List TODOs in base branch
        id: list_base
        run: |
          grep -r --exclude='no-todo-delete.sh' "// TODO:" . > base_todos.txt || true

      - name: Checkout PR branch
        uses: actions/checkout@v4
        with:
          ref: ${{ github.head_ref }}
          fetch-depth: 0

      - name: List TODOs in PR branch
        id: list_pr
        run: |
          grep -r --exclude='no-todo-delete.sh' "// TODO:" . > pr_todos.txt || true

      - name: Determine TODO Changes
        id: determine_changes
        run: |
          new_todos=$(comm -13 <(sort base_todos.txt) <(sort pr_todos.txt))
          removed_todos=$(comm -23 <(sort base_todos.txt) <(sort pr_todos.txt))

          echo "new_todos=$new_todos" >> $GITHUB_ENV
          echo "removed_todos=$removed_todos" >> $GITHUB_ENV

      - name: Add or Update Comment
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GH_REPO: ${{ github.repository }}
          NUMBER: ${{ github.event.pull_request.number }}
          NEW_TODOS: ${{ env.new_todos }}
          REMOVED_TODOS: ${{ env.removed_todos }}
        run: |
          # Construct the comment body
          BODY=$(cat <<EOF
          <!-- TODO-Counter-Comment -->
          **TODO Changes**

          $(if [ ! -z "$NEW_TODOS" ]; then echo "new:"; echo "$NEW_TODOS" | sed 's/^/- /'; echo ""; fi)

          $(if [ ! -z "$REMOVED_TODOS" ]; then echo "removed:"; echo "$REMOVED_TODOS" | sed 's/^/- /'; echo ""; fi)
          EOF
          )

          # Find the existing comment ID
          comment_id=$(gh api repos/$GH_REPO/issues/$NUMBER/comments --jq ".[] | select(.body | contains(\"<!-- TODO-Counter-Comment -->\")) | .id")

          if [ -n "$comment_id" ]; then
            # Update the existing comment
            gh api repos/$GH_REPO/issues/comments/$comment_id -X PATCH -f body="$BODY"
          else
            # Create a new comment if not found
            gh pr comment "$NUMBER" --body "$BODY"
          fi
        shell: bash
