/linear-api/

# env files
.env*
!.env.schema

# misc
/dump/
*.pem

# Claude Code — settings.json is shared and versioned; the .local variant is
# per-machine and must never be committed (it records approved commands verbatim,
# which is how credentials have leaked into this file in the past).
.claude/settings.local.json

# Apple
.DS_Store
/node_modules/

# React Router
/.react-router/
/build/

# Playwright
node_modules/
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/
test-results/
/playwright/.auth/*
playwright/.auth/*
e2e/.auth/
.vercel/
.vscode/
# Superpowers brainstorm artifacts
.superpowers/
