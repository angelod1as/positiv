.react-router
build
node_modules
README.md

# The Sanity Studio deploys to Sanity's own hosting, never with the app.
studio

# Never let local secrets reach a build layer. .env.schema holds no values and
# is needed by varlock at runtime, so it stays.
.env
.env.*
!.env.schema
